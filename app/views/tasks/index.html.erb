<h1>タスク一覧</h1>
<%= link_to "新規タスク登録", new_task_path %>

<% if @tasks.any? %>

<ul>
    <% @tasks.each do |task| %>
        <li><%= link_to task.id, task_path(task) %> : &lt;<%= task.status %>&gt; <%= task.content %></li>
    <% end %>
</ul>

<% else %>

<p>タスクがありません</p>

<% end %>


